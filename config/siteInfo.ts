interface SiteConfig {
    siteTitle: string;
    siteDescription: string;
    contactEmail: string;
}

const siteConfig: SiteConfig = {
    siteTitle: 'Gitto Blog',
    siteDescription: 'Gitto Blog',
    contactEmail: 'gittoyong.gmail.com'
}

export default siteConfig